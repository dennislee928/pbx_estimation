{
  "name": "pbx-rag-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node --check src/worker.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
